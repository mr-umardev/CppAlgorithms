{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",  // or "gcc" depending on your compiler
            "args": ["-g", "main.cpp", "-o", "main"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
